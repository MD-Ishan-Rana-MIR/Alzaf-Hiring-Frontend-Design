# In this project to api call i am useing nextjs, tailwind css and shadcdn and api call useing fetch.

# But unfortunately when fetching data and call api show cros error.I am also mail you.

# Sort options: price-asc, price-desc name-asc, name-desc rating-asc, rating-desc not clear information api docs how can i do this api and send query data.

# Clone the project and node module install command npm install than run project npm run dev.